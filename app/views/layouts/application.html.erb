<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || 'Lukas Trends' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'tailwind', 'inter-font', 'data-turbo-track': 'reload' %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/echarts/dist/echarts.min.js">
      console.log("Holi")
    </script>
  </head>

  <body>
    <nav class="flex items-center justify-between bg-white mx-auto w-[95vw] py-2 pr-2 border-b shadow-[0_4px_4px_-2px_rgba(0,0,0,0.1)]">
      <!-- Logo Section -->
      <div class="flex items-center space-x-2">
        <img src="/icon.png" alt="Logo" class="w-10 h-10">
        <span class="text-xl font-bold text-blue-600">LukasTrend</span>
      </div>

      <!-- Links Section -->
      <div class="flex space-x-4">
        <!-- Sign out Button -->
        <% if user_signed_in? %>
          <div class="flex gap-4 items-center">
            <%= image_tag current_user.avatar_url, class: "size-10 rounded-full" %>
            <%= button_to destroy_user_session_path, method: :delete, class: "flex items-center" do %>
              <span class="sr-only">Sign out</span>
              <%= image_tag('log-out-icon.svg') %>
            <% end %>
          </div>
        <% else %>
        <!-- Log in Button -->
          <%= link_to new_user_session_path, class: 'flex items-center px-4 py-2 border border-gray-300 text-gray-700 rounded-lg shadow-sm hover:bg-gray-100 transition text-xs' do %>
            <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 9.75A3.75 3.75 0 1112 6a3.75 3.75 0 013.75 3.75zM6.75 22.5a6 6 0 0112 0" />
            </svg>
            Log in
          <% end %>

          <!-- Sign up Button -->
          <%= link_to new_user_registration_path, class: 'flex items-center px-4 py-2 bg-blue-600 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 transition text-xs' do %>
            Sign up
          <% end %>
        <% end %>
      </div>
    </nav>
    <main class="container mx-auto mt-28 px-5"><%= yield %></main>
  </body>
  <script src="https://cdn.jsdelivr.net/npm/echarts@5.3.1/dist/echarts.min.js"></script>
</html>
